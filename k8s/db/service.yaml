apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: invoices
  labels:
    app: invoices
    component: db
spec:
  selector:
    app: invoices
    component: db
  ports:
    - port: 3306
      targetPort: 3306
